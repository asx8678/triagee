# Assets

The triangle mark is extracted from the approved prototype. Keep the existing production logo asset when it represents the same PTV Triage mark; do not redraw the brand or replace it with a generic security icon. Reference icon SVGs are also exported by `qa/capture_reference.py`.

No font files are distributed. The exact reference font stack is in `../source/approved-exact.css`. Use the current application's licensed local fonts where appropriate and verify the actual rendered font. Do not add a remote font or CDN requirement.
