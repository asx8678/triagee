# OS junk
.DS_Store

# Dependencies (the staged snapshot symlinks to the legacy app — never publish it)
node_modules/
tmp/cve-collector/node_modules

# Secrets and databases — defense in depth
.env
.env.*
!.env.example
*.pat
*.pem
*.key
*.p12
*.pfx
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm
*.dump
*.sql
reports/

# Local verification output (existing tracked reports remain tracked)
/evidence/

# Local agent harness journals (tracked harness configuration remains untouched)
.pi/fabric/mesh/events.jsonl
.pi/fabric/mesh/sequence
