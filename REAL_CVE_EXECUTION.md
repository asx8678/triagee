# Real CVE replacement — implementation and acceptance ledger

## Verified completion — 2026-09-16

**Source/data integration complete; existing application database replacement is blocked.**

- Downloaded catalogue: `app/priv/reference/nvd-cves.json`, exactly 95 unique CVEs: 30 CRITICAL, 40 HIGH, 20 MEDIUM, 5 LOW. Records are re-derived from retained NVD response bodies and SHA-256 receipts. Publication sample is 2025 Q1; no latest-feed claim.
- Development entry `app/priv/repo/seeds.exs` now calls `Triage.ReferenceData.seed!/0`. Synthetic fixtures remain test-only. The loader is offline, transactional under the import lock, repeatable, and retires only owned demo/reference placements while retaining case/review/exception history and unrelated imported data.
- `mix triage.reference --check|--download|--preview|--apply --database NAME` is registered. Catalogue validation rejects incomplete/tampered records and unsafe partial replacements. CVE/finding pages and saved cases explicitly label NVD reference data, provide real source links/CVSS evidence, and do not assert deployed vulnerable versions or fixes.
- Focused database/UI and catalogue verification: **51 passed**, `evidence/real-cves/targeted.log`; owned database `triage_test_ab_real_20260916171614_199281` dropped and cluster stopped.
- Full `mix precommit`: **823 passed, 2 skipped**, `evidence/real-cves/precommit.log`; owned database `triage_test_ab_real_20260916172037_207509` dropped and cluster stopped. The skips remain the pre-existing opt-in import-concurrency cases. Existing unused-helper warnings in `cve_presentation_test.exs` remain unrelated.
- Additional development build: `MIX_ENV=dev mise x -- mix compile --warnings-as-errors` passed and task help confirms registration.
- Runtime preview: **BLOCKED**. `MIX_ENV=dev mise x -- mix triage.reference --preview` reached configured `localhost:5432 / triage_dev` but SCRAM authentication failed because no PostgreSQL password is configured (`KeyError: key :password not found`). Exact evidence: `evidence/real-cves/runtime-preview.log`. No apply, migration, seed, reset or credential change was performed against working data.
- Once authorized local DB access is configured, run from `app/`: `mise x -- mix triage.reference --preview`, then `mise x -- mix triage.reference --apply --database triage_dev` (or the actual intended configured database name). Operator procedure: `app/REAL_CVES.md`.
- Verification is application/LiveView level, not a rendered-browser signoff. No commit, PR or deployment was performed. The original checklist below is retained as historical planning, not as the current completion status.

User asks to download real CVEs from the internet and replace fake CVEs in the active triage inventory. Exact agreed selection: 30 CRITICAL, 40 HIGH, 20 MEDIUM, 5 LOW = 95 DISTINCT CVEs. They did not specify medium/low counts; Main stated these defaults. Do not invent IDs, scores, descriptions or affected package mappings. Do not just write a plan or return speculative data.

## Historical starting state (superseded by verification above)

Main added `app/lib/triage/cve_catalog.ex`: explicit Req-based NVD download and normalization (NVD publication window 2025 Q1, CVSS v3 metric provenance, rate-spaced four severity requests, bounded decode, no redirects). It has NOT been executed or tested yet. Review/improve it and use actual internet responses; fail closed if counts cannot be met. Other new CVE exception source from previous task is implemented and previously tested (808 passed +2 skipped); preserve it and existing uncommitted closeout work. Do not modify harness `.pi/` state or recursively delegate.

Current development entry `app/priv/repo/seeds.exs` calls `Triage.Seeds.seed()` which inserts fabricated advisory mappings. Hundreds of existing tests use those deterministic synthetic fixtures; keep those fixtures test-only, rather than replacing all test assertions with live internet content. `mix ecto.seed` / `mix setup` must load a checked-in downloaded real-reference catalogue OFFLINE by default; no network on application startup or page GET. Add explicit refresh/download CLI and safe local apply/replacement CLI/procedure.

## Original implementation checklist (historical; verified status above)

- [ ] Actually download NVD records; retain source URLs, fetch timestamps, CVSS v3 version/vector/score/metric-author, descriptions, publication/modification dates, canonical NVD links and real CPE affected-product evidence where available. Keep raw source receipts or hashes for mechanical verification. Include the normalized real catalogue in app/priv (no credentials).
- [ ] Strictly validate the entire local catalogue (shape, unique IDs, exact histogram, score bands, valid provenance, no rejected entries); no partial or silently padded/truncated success. Network failures or malformed/local tampered data must preserve previous file and database. Add bounded, explicit command options and fail nonzero on errors.
- [ ] Replace development seed entry with the real catalogue. Provide idempotent atomic loading/replacement, sharing the existing import advisory lock. Retire only exact known legacy synthetic seed-owned data, preserving unrelated imports and all saved cases/frozen snapshots/reviews/exception histories. Never globally delete by CVE ID alone — a fabricated seed ID may also identify a real unrelated advisory. Prefer retiring owned synthetic placements rather than deleting referenced history or claiming fake remediation.
- [ ] Data truthfulness: public CVEs are real, deployments are NOT discovered by a public feed. Create a clearly labelled reference/demo scope/container (e.g. `public-reference` / `not-a-deployment`) and prominent source labels, rather than claiming actual internet exposure, installed vulnerable versions, production affectedness or verified fixes. Product names/ranges must come from CPE/source text; unknown values remain explicit. Imported date is reference load time, not detection or publication. No invented scanner lifecycle or synthetic KEV/news presented as real intelligence. Old synthetic cache/news should be retired narrowly if appropriate.
- [ ] Active findings/CVE severity counters must show 30/40/20/5 for the reference dataset, without counting retired fake fixtures. `Inventory.list_groups` and `cve_summary_counts` already require active placement; `summary_counts` currently does not. Direct old finding/case history can remain readable but must not be presented as current real reference data. The unscoped `Inventory.fetch_cve` currently includes inactive-image unresolved occurrences: account for collisions with retired demo IDs without breaking historical evidence semantics.
- [ ] Surface catalogue provenance (NVD source, CVSS score, fetch time, and reference-only scope) in the relevant UI; fields cannot imply a scanner observed these public entries in a real deployment. Link each record to the real source. Keep earlier assessment/exception workflow functional for this explicitly demo/reference scope.
- [ ] Verify target runtime DB identity and preview replacement. User authorizes replacing known fake data, not deleting unrelated imported/security review history. No blind `mix ecto.reset`, truncate, force drops, or changes to other services. If the actual app DB is accessible and safely identified, apply the targeted replacement and verify exact active counts; if inaccessible, finish source/data/tests and clearly report that runtime application remains pending with the exact command/blocker. Do not claim the running UI was changed if only the code/file was changed.

## Original test checklist (historical; verified results above)

- [ ] Pure NVD parsing/selection, CVSS band/provenance, duplicate/rejected/invalid bodies, missing bucket and network failure tests with injected offline transport.
- [ ] Checked-in fixture verification: exactly95 unique valid CVE IDs and exact severity histogram, normalized rows mechanically derived from downloaded raw/source receipts, no fake description/version mappings.
- [ ] Disposable database test: start from old synthetic fixtures plus saved review/exception and unrelated imported data, apply real catalogue; reference counts exact, fake active findings retired, unrelated data/history unchanged, repeat load has no duplicate rows or invented extra history; invalid replacement is atomic/no-op.
- [ ] UI/LiveView tests: real CVE source links and reference banner visible, correct severity count/filter/paging, assessment and local-exception actions remain available, no network on GET.
- [ ] Targeted tests then full `mix precommit`; direct catalogue/runtime count probes, report actual outcomes and operational limitations. Never run product tests against the user's working DB.

## Repository/runtime notes

Read app/AGENTS.md and app/OWNED_DB_VERIFICATION.md. Fovea impact was run on seeds/inventory; Elixir graph has coverage gaps. Existing postgres binaries available read-only at `/home/adam/.local/postgresql-16/usr/lib/postgresql/16/bin`, libraries need `LD_LIBRARY_PATH=/home/adam/.local/postgresql-16/usr/lib/x86_64-linux-gnu`. Existing 5432 service was previously password-protected, so do not touch or assume ownership. `evidence/cve-triage/verify.sh` creates a unique disposable cluster and supports targeted test paths; full precommit calls coordinator-only `app/scripts/verify_owned_db.sh`. A child executor must not run that coordinator wrapper. No existing listeners/processes are to be stopped or replaced. New schema migrations (only if necessary) use `mix ecto.gen.migration` after task-help read. No commits/PRs/deployments authorized.

Main already traced `Seeds` source images (exact synthetic aaa111/bbbb2222/cccc3333 digests, registry.internal/app-a|app-b|shared-base), fake finding tuples and synthetic news source. Never guess ownership based on arbitrary CVE names. The old `Cases` evidence payload still describes synthetic_local_inventory even for imported records; ensure the new reference dataset is accurately labelled without rewriting old frozen snapshots or gratuitously invalidating all existing tests.
