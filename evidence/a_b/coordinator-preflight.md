# Coordinator preflight (fresh)

- `git rev-parse HEAD`: dc4843141b94aa8f9a040eec6aa0ea61978784cc; initial status retained. No Git mutation performed.
- Initial source hashing command exited 0; `find app/assets` warned directory absent (actual static source is app/priv/static). Supplement initial-static.paths/.sha256 records all 11 non-vendor static files before workers could modify them (outside worker ownership). Initial paths list 116 app source files. Correction discovered at integration: `set -e` stopped the grouped inventory after absent app/assets, omitting later app-root/root entries despite pipeline exit 0. The supplement and precommit manifests have separately labeled timing; do not claim an initial whole-tree hash. Dependencies/build/crash/credential/DB/harness contents deliberately excluded.
- Backup capture exit 2 stopped at app/.gitignore because its initial hash was absent (not because a worker changed it); app/.gitignore is unchanged. Original runtime/dev/test configs were captured with matching hashes before worker edits; mix.lock subsequently captured (hash comparison at integration). No originals restored or overwritten. Root .gitignore remains recoverable from HEAD; no checkpoint claim.
- Tool discovery command exited 1 at absent default-PATH elixir; recovered with installed `mise exec` (no downloads/install). `app/.mise.toml`: Erlang 27.3.4.16 / Elixir 1.20.4-otp-27. PostgreSQL CLI available at /opt/homebrew/opt/postgresql@18/bin/psql.
- `cd app && MIX_ENV=test MIX_TEST_PARTITION=_ab_help_10405 mise exec -- mix help precommit` then `mix help run` then `mix help test`: exit 0. Help only; no application/DB startup. precommit is mutating compile/deps.unlock/format/assets.setup/test; test alias creates/migrates/assets.setup before tests. Do not invoke either until owned-DB guard validates target.
- Fovea tools advertised ambiently but unavailable via discovery (no hits); used bounded pi.find/grep and explicit source reads instead, not alternate harness.
- Existing ports snapshot is evidence/a_b/initial-user-ports.txt (read-only lsof, no process stopping).
