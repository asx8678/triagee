# Independent probe notes (review in progress)
- routes-probe: 18/18 route/width pairs rendered; 16 LiveView pages connected, Overview is controller-rendered (no LiveView root expected). 1366x768 and 320x768. No document overflow. All nine mobile menus stayed collapsed after network-idle/connected mount. Case1 form top 419.73px; applicability bottom 529.73px at desktop.
- Read all 18 independent screenshots with pi.read. Also read matched before/after case desktop and before findings desktop. Before screenshots are SSR because original 127.0.0.1 origin socket was rejected; not proof of pre-redesign interactions.
- P2 reproduced: unknown valid scopes displayed as All and silently disappeared on unrelated form changes. scope-results.json records Findings typing busybox -> unscoped /findings?q=busybox; Activity changing environment -> unknown owner dropped. Reported immediately via mesh seq57; coordinator accepted/fixing. Two initial Activity native select attempts timed out; inspected DOM showed unchanged select and URL. Final Activity probe follows documented native select value/change recipe; Findings uses real mouse/typing. No product failure inferred from native driver timeouts.
- draft-results.json: real Tab/Enter/letter keys selected Unknown / Insufficient context / Investigation; inserted synthetic rationale. Original rev2/snapshot1/token survived reload. Refresh dialog canceled and focus returned. Native dirty navigation and save confirmations both canceled. Explicit Yes discard cleared draft; clean navigation succeeded. No save/refresh confirmation accepted. Browser paused on clean /findings for coordinator-only restart.
- Pure node --check app/priv/static/assets/js/app.js passed. 50/50 protected SHA-256 checks passed (protected-check.log); source-before manifest identifies intended redesign files, not an expectation all UI hashes remain unchanged.
- Reviewed test evidence: precommit-final.log ends 525 passed,2 skipped, guard exit0; target-1.log ends90 passed; concurrency.log separately2 passed. These are coordinator executions, not reviewer tests. Historical diagnostic failures remain evidence, not current passing results.
- Reviewed read-only browse, preview and unacknowledged-import fingerprints: identical within each pair. Applied synthetic import adds only an image; in-memory replay unchanged; explicit save adds one replay_runs receipt; history browse unchanged. No reviewer DB fingerprint generated (DB commands prohibited).
- Contrast report samples seven routes (not nine) and zero reported text failures/small input/button controls; border/control/focus states and screen reader behavior are not exhaustively covered. Shared tokens support readable contrast; this is not WCAG certification.
