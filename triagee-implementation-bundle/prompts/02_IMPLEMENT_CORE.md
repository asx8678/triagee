# M1 and M2 — Core implementation prompt

Implement the next unfinished core tasks T02–T09 in dependency order. Read specs 01–04 and 07, the concept gap list, the M0 audit, and current code. The primary goal is one actionable findings workflow, not another dashboard.

First remove unsafe defaults and surface real package fixes, images/environments, and existing work. Merge inventory, Review, and inspector in the real LiveView; make Findings the homepage; preserve draft/route/selection/operation state; add the compact exception register. Implement context-first, scope-local priority with SQL/application/count parity.

Then finish typed exceptions and verification through the correct domain boundaries. Not affected is not accepted_risk. Reported fixed is not verified. Add compatible storage/readers only where needed, preserving legacy chronology, boundary metadata, source provenance, cases, and history. Show due/invalidated exceptions and unresolved verification without generating duplicate work on identical scans.

Use the supplied acceptance IDs as evidence targets. Test server events and domain APIs, not only buttons. Exercise the real app with owned synthetic data and capture desktop/mobile/error flows. Do not port mock HTML scripts, auto-select all targets, return fake success, introduce a frontend rewrite, or bypass durable ticket recovery.

Deliver each milestone with exact changed files, task/test results, screenshots, data/route compatibility and rollback notes. Continue independent work around precise blockers; do not claim M2 complete while typed actions are placeholders.
